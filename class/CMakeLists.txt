#将当前目录中的源文件名称赋值给变量 DIR_CLASS_SRCS
AUX_SOURCE_DIRECTORY(. DIR_CLASS_SRCS)
#将 src 目录中的源文件编译为共享库
ADD_LIBRARY( class SHARED ${DIR_CLASS_SRCS})
#次使用 SET_TARGET_PROPERTIES 定义 CLEAN_DIRECT_OUTPUT属
SET_TARGET_PROPERTIES(class PROPERTIES CLEAN_DIRECT_OUTPUT 1)
